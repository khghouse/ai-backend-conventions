= Todo API Documentation
:doctype: book
:icons: font
:source-highlighter: highlightjs
:toc: left
:toclevels: 2
:sectlinks:

[[overview]]
= 개요

Todo API 문서입니다.

[[overview-http-verbs]]
== HTTP 동사

본 REST API에서 사용하는 HTTP 동사(verbs)는 가능한 한 표준 HTTP와 REST 규약을 따릅니다.

|===
| 동사 | 용례

| `GET`
| 리소스를 가져올 때 사용

| `POST`
| 새 리소스를 만들 때 사용

| `PUT`
| 기존 리소스를 수정할 때 사용

| `PATCH`
| 기존 리소스의 일부를 수정할 때 사용

| `DELETE`
| 기존 리소스를 삭제할 때 사용
|===

[[overview-http-status-codes]]
== HTTP 상태 코드

본 REST API에서 사용하는 HTTP 상태 코드는 가능한 한 표준 HTTP와 REST 규약을 따릅니다.

|===
| 상태 코드 | 용례

| `200 OK`
| 요청을 성공적으로 처리함

| `400 Bad Request`
| 잘못된 요청 (유효성 검사 실패)

| `401 Unauthorized`
| 인증 실패

| `403 Forbidden`
| 권한 없음

| `404 Not Found`
| 요청한 리소스가 없음

| `409 Conflict`
| 데이터 중복 및 상태 충돌

| `422 Unprocessable Entity`
| 비즈니스 로직 / 정책 위반

| `500 Internal Server Error`
| 서버 내부 오류
|===

[[overview-errors]]
== 오류

에러 응답이 발생했을 때 (상태 코드 >= 400), 본문에 해당 문제를 기술한 JSON 객체가 담겨있습니다.

include::{snippets}/common/error-response/response-fields.adoc[]

[[resources]]
= 리소스

[[resources-todos]]
== Todo

Todo 리소스는 할 일을 관리하는 API입니다.

[[resources-todos-list]]
=== Todo 목록 조회

`GET` 요청을 사용하여 서비스의 모든 Todo를 조회할 수 있습니다.

operation::todo-controller-docs-test/get-todos[snippets='response-fields,http-response,response-body']

[[resources-todos-create]]
=== Todo 생성

`POST` 요청을 사용해서 새 Todo를 만들 수 있습니다.

operation::todo-controller-docs-test/create-todo[snippets='request-fields,response-fields,http-request,http-response,request-body,response-body']

[[resources-todos-retrieve]]
=== Todo 조회

`GET` 요청을 사용해서 기존 Todo 하나를 조회할 수 있습니다.

operation::todo-controller-docs-test/get-todo[snippets='path-parameters,response-fields,http-request,http-response,response-body']

[[resources-todos-update]]
=== Todo 수정

`PUT` 요청을 사용해서 기존 Todo를 수정할 수 있습니다.

operation::todo-controller-docs-test/update-todo[snippets='path-parameters,request-fields,response-fields,http-request,http-response,request-body,response-body']

[[resources-todos-complete]]
=== Todo 완료

`PATCH` 요청을 사용해서 Todo를 완료 처리할 수 있습니다.

operation::todo-controller-docs-test/complete-todo[snippets='path-parameters,response-fields,http-request,http-response,response-body']

[[resources-todos-delete]]
=== Todo 삭제

`DELETE` 요청을 사용해서 기존 Todo를 삭제할 수 있습니다.

operation::todo-controller-docs-test/delete-todo[snippets='path-parameters,response-fields,http-request,http-response,response-body']
